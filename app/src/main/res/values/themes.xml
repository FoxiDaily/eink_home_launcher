<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EinkHome" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">#444444</item>
        <item name="colorPrimary">@android:color/black</item>
    </style>

    <style name="Theme.EinkHome" parent="Base.Theme.EinkHome" />

    <!-- 圆形遮罩样式 - 墨水屏建议少用，保持方正或大圆角 -->
    <style name="CircleShapeAppearance" parent="">
        <item name="cornerSize">0dp</item>
    </style>
</resources>